<h2 class="page-header">Auctions</h2>
<div id="listOfAuctions">
<div class="card-deck">
{{#each auctions}}
{{#dividedby3 @index}}</div>{{/dividedby3}}
{{#dividedby3 @index}}<div class="card-deck" style="margin-top:20px;">{{/dividedby3}}
<div class="card" >
  <div class="card-body">
    <h4 class="card-title">{{this.name}}</h4>
    <p class="card-text">{{this.description}}</p>
    <p class="card-text">Price: {{this.price}}</p>
    <a href="/auction/{{this._id}}" class="btn btn-primary">Go to auction</a>
  </div>
</div>
{{/each}}
</div>
</div>
<div class="center-block" style="width:40%;">
  <button id="paginationButton" type="submit" class="btn btn-default">Load more</button>
</div>
